{% import '__macros/html.twig' as html %}

<div class="border p-3 mb-3 bg-light">
	{{ html.definitions([
		{label: 'Identifier', value: client.identifier},
		{label: 'Client Name', value: client.name},
		{label: 'Description', value: client.description}
	]) }}
</div>
